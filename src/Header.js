import React from 'react';
import Menu from "./Menu";

function Header(props) {
    return (
        <header>
            <h3>
                Cover
            </h3>
            <Menu />
        </header>
    );
}

export default Header;